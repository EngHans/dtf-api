type Query {
  getDataFromAPI (
      order: orderObject
      pagination: paginationObject
    )
  : DataResponse
  getLastDateFromAPI (
      order: orderObject
      pagination: paginationObject
    )
  : DataResponse
}
